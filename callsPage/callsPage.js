const callsPage = () => {
  return `
    <div class="calls-page">
      <div>
        <p>To start calling contacts who have WhatsApp, tap at the bottom of your screeen</p>
      </div>
    </div>
  `
}

export default callsPage
